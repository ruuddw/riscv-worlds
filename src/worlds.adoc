:le: &#8804;
:ge: &#8805;
:imagesdir: ./images

[[worlds]]

== RISC-V Worlds

_RISC-V Worlds_ provides isolation in a hardware platform by constraining
access to system physical addresses. It provides _Worlds_, which are system
partitions consisting of execution contexts (including agents, such as harts
and devices, that can initiate a transaction on a physical address within a
world) and resources (such as memories and peripheral devices, which respond to
transactions at a physical address within a world). Worlds are created and
configured by a trusted execution environment, usually at system boot time.

Worlds are uniquely identified by a _World_ _Identifier_ (_WID_) that
is associated with all outgoing transactions.
The maximum number of unique WIDs on a platform is _NWorlds_. Increasing
NWorlds increases the hardware cost, and in practice, 2-8 WIDs (requiring 1-3
bits to represent) are sufficient for many use cases.

Hardware agents (harts and devices) that can initiate transactions on physical
addresses may support multiple contexts, with each context potentially being in
a different world. As the WID to associate with outgoing transactions is
typically determined by a control register, a given (software or hardware)
context can only be present in one world at a time.

Resources are identified by system physical addresses. A world may be granted
some combination of access permissions on a physical
address, meaning that emitted transactions will be examined by a WID-aware
interface somewhere along the transaction path to enforce such permission
checking. It is possible for a resource to be accessible via more than one WID,
allowing for resource-sharing across worlds.

_RISC-V Worlds_ is designed for the case where the allocation of
agent contexts and resources to worlds is performed before or at
reset/boot time, and not changed dynamically when the system is running
unless there is a system reset.  Efficiently changing worlds allocation
configurations dynamically while the system is running is not a goal of
the current specification.

The method of propagating and checking the WID on busses
is platform-specific, with different bus fabrics supporting WIDs in
bus-specific ways. The manner in which rejected transactions are reported
is also platform-specific.

NOTE: Theoretically all permissions checks on transactions could be
performed at the source agent to prevent any illegal transactions from
entering the bus fabric. But in practice, replicating and checking the
entire platform permissions map at each agent is prohibitively
expensive, particularly when permissions are configurable, and so it is
assumed that permissions checking is distributed out in the bus fabric and
attached resources.

Any cache on the path that may allocate data into cache lines must
track the WID of the original transaction.  How the WID is stored (e.g., as
part of the cache tag), whether cache lines are shared across worlds or
isolated per world, and the cache replacement policy are all platform-specific.

=== RISC-V ISA World-ID Extensions

RISC-V harts that support Worlds associate a WID with all memory
accesses initiated by that hart. The World-ID extensions allow
different privilege modes on a hart to be tagged with different WIDs.

There are the following levels of Worlds support on RISC-V harts.

* Without any Worlds ISA extensions enabled, a Worlds-capable implementation
  will fix the WID to a platform-defined value for the outgoing transactions of
  all privilege modes on a hart. This platform-defined value is referred to as
  `pmwid`.
* With the *Smwid* extension, the `mwid` CSR is introduced to provide a
  lockable control of the WID value assigned to the hart. A platform-defined
  value, `pmwidlist`, may limit the set of WIDs that can be used with `mwid`.
** If *Smwid* is enabled, the platform determines whether `mwid` is locked or
   unlocked out of reset.
** It is platform-specific whether (or not) `mwid` changes between resets.
* The *Smlwid* extension introduces the `mlwid` CSR to control the WID of
  lower-privilege modes. A platform-defined value, `pmlwidlist`, may limit the
  set of WIDs that can be used with `mlwid`.
* The *Smlwidlist* extension depends on the *Smwid* and *Smlwid* extensions, it
  introduces the `mlwidlist` CSR, where WID _i_ is represented by bit _i_ of the
  register, to allow M-mode to restrict the set of WIDs that may be used with
  `mlwid`. The `mlwidlist` CSR is locked (read-only) whenever `mwid` is locked.
* The *Smwiddeleg* extension, which further enables M-mode to delegate to lower privilege
  levels the ability to assign the WID of the next lower privilege level, thereby adding
  the *Sswid* extension. With the H extension, this means HS-mode may set the WID for
  VS-mode and lower.
* The *Shlwid/Shwiddeleg* extensions add another delegation level to match the additional
  level of privilege hierarchy that the H extension introduces. Two HS-mode
  registers (that are not visible to VS) are added, called `hlwid` and `hwiddeleg`.
  *Shwiddeleg* also adds a VS replica CSR called `vslwid` which is accessed from HS-mode
  as well as via the `slwid` CSR in VS-mode (see
   <<hypervisor.adoc#_hypervisor_and_virtual_supervisor_csrs, VS CSRs in the Hypervisor
   chapter>>).

All accesses, including implicit memory references such as instruction
fetches and page-table walks, must be tagged with a WID (either specified by the
platform, or by the RISC-V Worlds CSRs).

*World-ID extensions do not allow a privilege mode to change its own WID.* The
only exception to this rule is the `mwid` CSR that, if unlocked on-reset,
allows M-mode to set and lock the WID that applies to itself. This is
thematically consistent with lockable registers in other extensions (e.g. PMP),
where early-boot M-mode may be operating as a root-of-trust, with a temporal
boundary to separate it from the subsequent "run-time" M-mode firmware. In this
way, early-boot M-mode firmware (that is, code executing before the
M-mode-constraining registers are locked) is considered to be more privileged
than regular M-mode (after the constraints are locked), and in this way it
still respects the spirit of the aforementioned rule.

NOTE: Different harts in a system may have different WIDs in M-mode.

=== RISC-V Worlds CSRs

The CSRs for the RISC-V Worlds extensions are listed in the following table.

[cols="1,1,^1,^1,5"]
.World-ID CSRs
|===
|Size in bits|Register|Access|Proposed CSR Address|Description
| `MXLEN` | `mwid` | M | `0x749` | WID used for M-mode with the optional
Smwid extension. +
`Ceil(Log~2~NWorlds)` LSBs are used, other bits are reserved (read-only zeros)
except for the lock bit at offset `XLEN-1`.
| `MXLEN` | `mlwid` | M | `0x390` | WID used for lower-than-M privilege
levels. +
`Ceil(Log~2~NWorlds)` LSBs are used, other bits are reserved (read-only zeros).
| `MXLEN` | `mlwidlist` | M | `0x74A` | Set of WID values delegated to
M-mode, represented as a bit vector. +
`NWorlds` LSBs are used, other bits are reserved (read-only zeros). +
Register is locked (read-only) whenever `mwid` is locked.
| `MXLEN` | `mwiddeleg` | M | `0x748` | Set of WID
values delegated to (H)S-mode, represented as a bit vector. `NWorlds` LSBs
are used, other bits are reserved (read-only zeros).
| `HSXLEN` | `hlwid` | HS | `???` | WID value used for lower-than-HS
privilege levels. `Ceil(Log~2~NWorlds)` LSBs are used, others are zero.
| `HSXLEN` | `hwiddeleg` | HS | `???` | Set of WID values delegated to
VS-mode, represented as a bit vector. `NWorlds` LSBs are used, others are zero.
| `SXLEN` | `slwid` | S, HS | `0x190` | WID value used in lower-than-(V)S
privilege levels. +
`Ceil(Log~2~NWorlds)` LSBs are used, other bits are reserved
(read-only zeros).
| `VSXLEN` | `vslwid` | HS | `???` | Virtualization replica register of
the slwid register. WID value used in VU-mode. `Ceil(Log~2~NWorlds)`
LSBs are used, others are zero.
|===

NOTE: Like for other VS replica registers, in HS-mode the `vslwid` register can
be programmed to set the effective WID of VU mode. In VS-mode accesses to
`slwid` reflect the `vslwid` contents.

All RISC-V Worlds extensions support a maximum of `XLEN` worlds.

=== One world per hart (no ISA extension)

When there is only one world per hart, there are no ISA-visible additions to
the RISC-V hart.  The hart is reset into a single world and all transactions
from that hart, regardless of privilege mode, are tagged with the WID of that
world. This WID is platform-defined, we refer to it as `pmwid`, and it might
take any non-ISA form (external to the hart). E.g. pinstrap, fuse, SoC
register, etc. The assignment of WIDs to harts, and whether (and how) a hart
is able to determine any information about Worlds configuration, is
platform-specific.

.One world with no ISA extension
image::drawio/world_none.svg[WorldNone,align="center"]

=== One world per hart with the Smwid extension

The Smwid extension introduces the lockable `mwid` CSR. This CSR provides a
view of the WID value for all outgoing transactions and includes a sticky
lock-bit to determine its mutability. The lock bit (`L`) is at offset `MXLEN-1`
within the CSR. The WID field is WARL and occupies the least-significant
Ceil(Log~2~NWorlds) bits of the CSR. The remaining bits are non-zero reserved (NZR).
SW should write 0 to get the behavior described in this spec, while future
extensions might define non-zero values as causing some different behavior.
Implementations should make NZR fields read-only zero unless they implement additional
extensions that use the reserved encodings. The register is formatted as
shown in <<MWID64>> when MXLEN=64 and <<MWID32>> when MXLEN=32.

[[MWID64]]
.Machine WID value for M-privilege mode Register (`mwid`) for MXLEN=64

[wavedrom, , ]
....
{reg: [
  {bits: 6, name: 'WID'},
  {bits: 57, name: 'NZR'},
  {bits: 1, name: 'L'},
], config:{lanes: 2, hspace:1024}}
....

[[MWID32]]
.Machine WID value for M-privilege mode Register (`mwid`) for MXLEN=32

[wavedrom, , ]
....
{reg: [
  {bits: 5, name: 'WID'},
  {bits: 26, name: 'NZR'},
  {bits: 1, name: 'L'},
], config:{lanes: 1, hspace:1024}}
....

The lock bit is sticky, meaning that once set it cannot be unset until the
hart is reset.  Once the `mwid` CSR is locked (by setting `L` to 1), any attempt
to write to the CSR is ignored. The CSR becomes read-only.

On-reset, the `mwid` CSR is expected to show the platform-defined `pmwid` value.
The `mwid` CSR may already be locked immediately out of reset, or the platform may
leave it unlocked in the case that early-boot M-mode code is responsible for
setting the WID (and locking it, presumably).

The platform may limit the set of WIDs that may be used with `mwid` via a
non-ISA `pmwidlist` mask. `pmwid` must be included in `pmwidlist`.
Assigning a WID value to `mwid` that is not enabled by that `pmwidlist` mask
will result in the hart raising an exception. This will
result in a complete hang of the hart (as it will be unable to fetch
instructions), which is the appropriate outcome if M-mode attempts to
contravene platform-imposed constraints.

.One world with the Smwid extension
image::drawio/Smwid.svg[Smwid,align="center"]

=== Smlwid extension

The Smlwid extension adds support for M-mode to control the world used by
less-privileged modes, and can only be added to harts with at least two
privilege modes.

This extension adds `mlwid`, which is an M-mode WARL read-write CSR, whose
Ceil(Log~2~NWorlds) least-significant bits set the WID to be used by
lower-privilege modes.

NOTE: Smlwid is not dependent on the Smwid extension.

NOTE: If the system supports demand-paged virtual memory, then any
address-translation caches must ensure that translations are cached
separately for each WID.  A simple implementation can flush
address-translation caches on any `mlwid` write.

It is platform-specific which worlds can be used by lower-privilege modes
on this hart. This may be constrained by a non-ISA `pmlwidlist` mask.
`pmlwidlist` (width of NWorlds) represents a set of WIDs as a bit vector,
with WID _i_ represented by bit _i_ of the register. Each bit indicates whether
the corresponding WID is authorized for use with `mlwid`.

NOTE: There is no HW discovery mechanism for M-mode software to discover the
available worlds and their permissions (besides raising exceptions). Information
about worlds and resource assignment must be conveyed by software mechanisms.
The platform software will have predetermined allocations for the worlds on a
platform and all required information needed by M-mode software must be
provided out-of-band.

If an unauthorized WID is written to `mlwid`, meaning that the WID value is not
enabled in the platform's `pmlwidlist` mask, then the lower-privilege mode's
first attempt to fetch an instruction will raise a precise exception.  This
exception will indicate the "software-check" cause.
Similarly, after an MPRV update in `mstatus` to
modify the effective privilege mode for loads and stores executed in M-mode
(for them to take effect as S or U mode accesses), then the first such load or
store will raise a precise software-check exception if `mlwid` is not
authorized.

When this exception is raised, the `mtval` register is set to "illegal world
fault (code=4)". The software-check exception caused by Smlwid has higher
priority than any access-fault exception (See <<mcauses>> of
Privileged Specification). For
example, if M-mode uses MPRV to issue a transaction as S or U mode,
that transaction may be eligible to generate an access fault (due
to MMU and/or PMP constraints) _as well as_ a software-check exception (due to
an unauthorized WID) - in that case, the latter software-check exception has
priority and supercedes the access fault.

.Smlwid extension: Support for M-mode to control lower privilege modes' world, shown here without Smwid.
image::drawio/Smlwid.svg[Smlwid_none,align="center"]

M-mode may have a different WID than any assignable by `mlwid` to
lower-privilege modes.

NOTE: The platform is not required to allow lower-privilege modes to be in the
same world as M-mode. `pmwid` may not be included in `pmlwidlist`. It is up to
the platform to determine whether or not to allow this.

=== Smlwidlist extension

The Smlwidlist extension depends on the Smwid and Smlwid extensions, it
introduces the `mlwidlist` CSR to allow M-mode to further restrict the set of
WIDs that may be used with `mlwid` (beyond any limitations already imposed by
the platform via the `pmlwidlist` mask). Like `pmlwidlist`, `mlwidlist` (width
of NWorlds) represents a set of WIDs as a bit vector, with WID _i_ represented
by bit _i_ of the register.

On-reset, the `mlwidlist` CSR is required to show the platform-defined
`pmlwidlist` value.

The `mlwidlist` CSR is locked (read-only) whenever `mwid` is locked, it does
not have a distinct lock bit.

.Smlwidlist extension, shown here with the Smwid and Smlwid dependencies.
image::drawio/Smlwidlist.svg[Smlwidlist,align="center"]

The `mlwidlist` mask is WARL such that any attempts to enable a WID in the mask
that is not enabled in the platform's `pmlwidlist` will be overriden. I.e. a
write to `mlwidlist` followed by a read will return the result of a bitwise-AND
between the written value and the platform mask.

=== Smwiddeleg / Sswid extensions

The Smwiddeleg extension requires the Smlwid extension and allows M-mode to
delegate to S-mode the ability to allocate WIDs to privilege modes
lower than S. The Smwiddeleg extension optionally enables the Sswid extension
for S-mode.

NOTE: these extensions are not dependent on Smwid nor Smlwidlist.

The Smwiddeleg extension adds the `mwiddeleg` M-mode read-write CSR. The
`mwiddeleg` register represents a set of WIDs as a bit vector with
WID _i_ represented by bit _i_ of the register. The `mwiddeleg` CSR
is a WARL register where each bit that can be set indicates a WID that
is delegated to S-mode. The set of worlds that can be delegated to
S-mode on a hart is platform-specific.

NOTE: Different harts on a platform can have different sets of
delegable worlds.

`mwiddeleg` is always a subset of `mlwidlist` (if Smlwidlist is enabled) and
`pmlwidlist` (if implemented by the platform). Attempts to authorize a WID in
`mwiddeleg` that is not authorized in `mlwidlist`/`pmlwidlist` will fail - the
WARL behavior will mask off any such bits. Similarly, if `mlwidlist` is updated,
any WIDs that are de-authorized (set to zero) will be implicitly de-authorized
in `mwiddeleg` also.

If `mwiddeleg` is zero (which may happen even if non-zero values are written to
it, see the previous paragraph), the applicable lower privilege level extensions
(Shlwid, Shwiddeleg, Sswid) are disabled, in which case any accesses to the
`hlwid`, `hwiddeleg`, `slwid` or `vslwid` CSR raises an illegal instruction exception.
Otherwise, if `mwiddeleg` is non-zero, the Sswid extension is enabled. Like
any other WARL register, M-mode software is expected to read back the register
to determine the actual configuration retained by the implementation.

The *Sswid* extension adds the S-mode read-write `slwid` CSR, which
sets the WID used for modes lower than S-mode.  The `slwid` field is WARL
with the WID encoded in the Ceil(Log~2~NWorlds) least-significant bits.

If an unauthorized WID (per the `mwiddeleg` list) is written to `slwid`, then
the first attempt to fetch an instruction by U-mode will raise a precise
exception indicating the software-check cause.  Similarly, after a MPRV update,
any attempt by M-mode to perform a load or store "as U-mode" will be subject to
the same `mwiddeleg` authorization check, meaning that it may also raise a
precise exception (indicating the software-check cause).

When this exception is raised, the `mtval` register is set to "illegal world
fault (code=4)".  The software-check exception caused by Sswid has higher
priority than any access-fault exception (See <<mcauses>> of 
Privileged Specification).

.Smwiddeleg/Sswid extensions: Support for S-mode to control U-mode's world. Shown here without Smwid or Smlwidlist.
image::drawio/Smwiddeleg_Sswid.svg[Smwiddeleg_Sswid,align="center"]

NOTE: If the system supports demand-paged virtual memory, then any
address-translation caches must ensure that translations are cached
separately for each WID.  A simple implementation can flush
address-translation caches on any write to `mwiddeleg` or `slwid`.

There is no requirement for `mwiddeleg` to contain the WID in `mlwid`,
i.e., S-mode can be set to a different world than the ones it is
allowed to assign to U-mode through `slwid` (when Sswid is enabled).

On reset, `mwiddeleg` is zero and hence Sswid and Shwiddeleg/Shlwid are disabled.

=== Shlwid / Shwiddeleg extensions

The Shlwid extension adds the HS-mode read-write `hlwid` CSR, which
sets the WID used for VS-mode and, if not further delegated, for VU-mode. The `hlwid`
field is WARL, whose Ceil(Log~2~NWorlds) least-significant bits set the WID to be used by
lower-privilege modes.

If an unauthorized WID (per the `mwiddeleg` list) is written to `hlwid`, then
the first attempt to fetch an instruction by VS-mode (or VU-mode if VS delegation
is disabled) will take a precise exception indicating the software-check cause.
Any attempt to perform a hypervisor load or store instruction, or M-mode access with MPRV
and MPV set, will be subject to the same `hwiddeleg` authorization check. This means
that it may also take a precise exception (indicating the software-check cause).

When this exception is raised, the `mtval` register is set to "illegal world
fault (code=4)". The software-check exception caused by Shlwid has higher
priority than any access-fault exception (See <<exception-priority>> of 
Privileged Specification) and leads to a trap being delivered to M-mode.

.Shwiddeleg/Shlwid extensions: Support for HS-mode to control the lower privilege mode's world.
image::drawio/Shwiddeleg_Shlwid.svg[Shwiddeleg_Shlwid,align="center"]

The Shwiddeleg extension requires the Smwdeleg/Shlwid/Sswid extensions and allows
HS-mode to delegate to VS-mode the ability to allocate WIDs to VU-mode.
The extension adds the `vslwid` replica CSR of `slwid` (see
 <<hypervisor.adoc#_hypervisor_and_virtual_supervisor_csrs, VS CSRs in the Hypervisor
   chapter>>).
The architectural state of `slwid` applies to U-mode, the architectural state of `vslwid`
applies to VU-mode. The `vslwid` state can be programmed from M, HS, and VS mode and it
sets the effective WID in VU mode. The Zicsr extension's instructions access `vslwid` instead
of `slwid` when executing with V=1.

NOTE: The Shwiddeleg extension is required because of the HU-bit. VS-mode could have
programmed `slwid` with the WID in VU-mode, but then the WID for VU-mode while in HU-mode
would need to be defined by another CSR.

The Shwiddeleg extension adds the `hwiddeleg` HS-mode read-write CSR. The
`hwiddeleg` register represents a set of WIDs as a bit vector with
WID _i_ represented by bit _i_ of the register.  The `hwiddeleg` CSR
is a WARL register where each bit that can be set indicates a WID that
is delegated to VS-mode. The set of worlds that can be delegated to
VS-mode on a hart is platform-specific.

NOTE: The `hwiddeleg` not only constraints `vslwid`, but as it is a virtualization
replica that takes over the function of `slwid` when V=1, it also defines the
authorized WIDs for the virtualized `slwid`. If an unauthorized WID is written
to `slwid` in VS-mode, also then the first attempt to fetch an instruction by
VU-mode will take a precise exception indicating the software-check cause.

`hwiddeleg` is always a subset of `mwiddeleg`. Attempts to authorize a WID in
`mwiddeleg` that is not authorized in `mwiddeleg` will fail - the
WARL behavior will mask off any such bits. Similarly, if `mwiddeleg` is updated
(either explicit or implicit), any WIDs that are de-authorized (set to zero)
will be implicitly de-authorized in `hwiddeleg` also.

If `hwiddeleg` is zero, the `vslwid` CSR is disabled, in which case
any accesses to the `vslwid` CSR raise an illegal instruction exception.

On reset, `hwiddeleg` is zero and hence `vslwid` is disabled.

=== Response to permission violations

When a hart attempts an explicit or implicit memory access that fails a World
permissions check, the access may or may not raise an access-fault exception of
the appropriate type (instruction-access fault, load-access fault, or
store/AMO-access fault). The ability to raise an appropriate exception will be
determined by where the permission-checking is being performed along the
transaction's path (in the hart itself, at the transaction's target, or
somewhere in between). When an access-fault exception cannot be raised, the
instruction performing the memory access can be retired but any writes to the
protected physical memory location are ignored and any memory reads return data
independent of the value in the protected physical memory location to avoid
violating memory isolation.

NOTE: Secure systems will typically ensure that some agent is notified
when an illegal access is rejected, even when an access-fault
exception cannot be raised on the hart context.

NOTE: We cannot require that reads that fail permissions checks but
that do not raise access-fault exceptions return a specific value
(e.g, zero) to the hart as this is incompatible with some
cache-coherence protocols, which may require cache-resident data be
modifiable even when the underlying physical memory locations are
protected and the bus responses previously returned zero.
